# 记忆翻牌（纯前端版）

技术栈：原生 HTML/CSS/JavaScript + Tailwind CDN

功能：4×4 记忆翻牌、3D 翻转动画、防连点、计时、计步、胜利弹窗、重新开始。

本版本不包含登录/排行榜/后端存储，保证可直接部署到 Vercel。

## 本地运行

直接用浏览器打开 `client/index.html` 即可。

## 部署到 Vercel

直接导入仓库部署即可（已提供 `vercel.json` 作为静态重写）。

